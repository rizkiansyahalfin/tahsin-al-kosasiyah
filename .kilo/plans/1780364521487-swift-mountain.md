# Plan: Update Landing Page with Registration Fee, Monthly Infaq, and WhatsApp Number

## Objective
Update the landing page to display:
1. Registration fee: 50 rb (Rp 50.000)
2. Monthly infaq: 35 rb/bulan (Rp 35.000 / bulan)
3. WhatsApp business number: 085772192024

## Current State Analysis
The landing page is implemented in `src/components/LandingPage.tsx` and includes:
- A highlight/pricing banner section (lines 243-271) showing start date and infaq amount
- WhatsApp number configured via `WA_NUMBER` constant (line 12) used throughout the component
- Multiple references to the WhatsApp number in various UI elements

## Required Changes

### 1. Update WhatsApp Business Number
- Change `WA_NUMBER` constant from `"6285697060001"` to `"6285772192024"`
- Update hardcoded display text in footer from "0856-9706-0001" to "085772192024"
- Verify all instances using `WA_LINK` will automatically update

### 2. Update Highlight/Pricing Banner Section
Replace current content (lines 243-271) with new registration and infaq information:
- Left section: "Uang Pendaftaran" with amount "Rp 50.000"
- Right section: "Infaq Bulanan" with amount "Rp 35.000 / bulan"

### 3. Update Related UI Elements
- Footer WhatsApp display text (line ~826)
- Any other hardcoded references to old WhatsApp number

## Implementation Details

### File: `src/components/LandingPage.tsx`

#### Changes:
1. Line 12: Update WA_NUMBER constant
   ```diff
   - const WA_NUMBER = "6285697060001";
   + const WA_NUMBER = "6285772192024";
   ```

2. Lines 252-269: Replace highlight section content
   ```diff
   -           <div className="flex items-center gap-5 w-full md:w-auto">
   -             <div className="w-16 h-16 bg-teal-50 rounded-2xl flex items-center justify-center shrink-0">
   -               <Calendar className="w-8 h-8 text-teal-600" />
   -             </div>
   -             <div>
   -               <p className="text-sm font-bold text-slate-400 uppercase tracking-wider mb-1">Mulai Belajar</p>
   -               <p className="text-2xl font-extrabold text-slate-800">15 Juli 2026</p>
   -             </div>
   -           </div>
   -           <div className="hidden md:block w-px h-16 bg-slate-200"></div>
   -           <div className="flex items-center gap-5 w-full md:w-auto bg-amber-50 p-4 rounded-2xl border border-amber-100 md:border-none md:bg-transparent md:p-0">
   -             <div className="w-16 h-16 bg-amber-100 rounded-2xl flex items-center justify-center shrink-0">
   -               <BookMarked className="w-8 h-8 text-amber-600" />
   -             </div>
   -             <div>
   -               <p className="text-sm font-bold text-amber-600 uppercase tracking-wider mb-1">Infaq Sangat Terjangkau</p>
   -               <p className="text-3xl font-extrabold text-slate-800">Rp 25.000 <span className="text-lg font-medium text-slate-500">/ bulan</span></p>
   -             </div>
   -           </div>
   +
   +           <div className="flex items-center gap-5 w-full md:w-auto">
   +             <div className="w-16 h-16 bg-teal-50 rounded-2xl flex items-center justify-center shrink-0">
   +               {/* Registration fee icon - to be selected based on availability */}
   +               <DollarSign className="w-8 h-8 text-teal-600" /> {/* Requires importing DollarSign from lucide-react */}
   +             </div>
   +             <div>
   +               <p className="text-sm font-bold text-slate-400 uppercase tracking-wider mb-1">Uang Pendaftaran</p>
   +               <p className="text-2xl font-extrabold text-slate-800">Rp 50.000</p>
   +             </div>
   +           </div>
   +           <div className="hidden md:block w-px h-16 bg-slate-200"></div>
   +           <div className="flex items-center gap-5 w-full md:w-auto bg-amber-50 p-4 rounded-2xl border border-amber-100 md:border-none md:bg-transparent md:p-0">
   +             <div className="w-16 h-16 bg-amber-100 rounded-2xl flex items-center justify-center shrink-0">
   +               {/* Infaq icon - to be selected based on availability */}
   +               <Gift className="w-8 h-8 text-amber-600" /> {/* Requires importing Gift from lucide-react - alternative: Donate, Banknote, or CreditCard */}
   +             </div>
   +             <div>
   +               <p className="text-sm font-bold text-amber-600 uppercase tracking-wider mb-1">Infaq Bulanan</p>
   +               <p className="text-3xl font-extrabold text-slate-800">Rp 35.000 <span className="text-lg font-medium text-slate-500">/ bulan</span></p>
   +             </div>
   +           </div>
   ```

3. Add required icon imports (lines 4-8):
   ```diff
   import { 
   -   BookOpen, Calendar, Clock, MapPin, Phone, Instagram, 
   -   CheckCircle, ArrowRight, User, Menu, X, 
   -   Users, Sparkles, BookMarked, MessageCircle, ChevronRight,
   -   GraduationCap, Award, PlayCircle
   +   BookOpen, Calendar, Clock, MapPin, Phone, Instagram, 
   +   CheckCircle, ArrowRight, User, Menu, X, 
   +   Users, Sparkles, BookMarked, MessageCircle, ChevronRight,
   +   GraduationCap, Award, PlayCircle, DollarSign, Gift
   + } from 'lucide-react';
   ```

   **Note:** If DollarSign or Gift icons are not available in the current lucide-react version, consider these alternatives:
   - For registration fee: CreditCard, Wallet, PiggyBank
   - For monthly infaq: Banknote, Donate, Wallet (different from registration), HandCoins

4. Footer update (around line 826):
   ```diff
   - <MessageCircle className="w-5 h-5 text-green-500" /> WhatsApp: 0856-9706-0001
   + <MessageCircle className="w-5 h-5 text-green-500" /> WhatsApp: 085772192024
   ```

## Dependencies
- No new dependencies required if DollarSign and Gift icons are available in lucide-react v0.300.0
- If icons unavailable, may need to select alternatives from existing icon set

## Testing Considerations
- Verify WhatsApp links open correct number (085772192024)
- Ensure responsive design works on mobile and desktop
- Check that all styling remains consistent
- Confirm no broken links or images
- Test that icon substitutions display correctly

## Estimated Effort
- 2-3 hours for implementation and testing

## Next Steps
1. Verify icon availability in current lucide-react version
2. Implement changes in `src/components/LandingPage.tsx`
3. Test locally to verify functionality
4. Prepare for deployment